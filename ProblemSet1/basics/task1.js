/**
 * ARRAY TASK
 * Store random integer number in an array. Iterate over them and print them in separate line
 * Store random integer number in an array. Iterate over them and print them in same line
 * Store some names in array then print them in same line separated by space
 * Update values of the previous array and print them in separet line
 * Remove specific element from array (not index but the value)["yu","a"] > remove a
 * Concatenate two arrays (merge two arrays> no redundent values)
 * Convert array into set
 */